import type { Interfaces } from '@oclif/core';
import { Command } from '@oclif/core';
import type { FlagInput } from '@oclif/core/parser';
export type CommandFlags<Flags extends FlagInput> = Interfaces.InferredFlags<(typeof SequelizeCommand)['baseFlags'] & Flags>;
export declare abstract class SequelizeCommand<Flags extends FlagInput> extends Command {
    static strict: boolean;
    static baseFlags: {
        interactive: Interfaces.BooleanFlag<boolean>;
    };
    protected flags: CommandFlags<Flags>;
    init(): Promise<void>;
}
